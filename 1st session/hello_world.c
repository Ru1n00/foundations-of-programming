#include <stdio.h>  // stdio = standard input output

int main() {

    printf("Hello World!"); // print function - prints to console (terminal) anything inside the parenthesis

    return 0;  // return 0 - means the program ran successfully
}

// anything after // is a comment and is ignored by the compiler